import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        brandGreen: '#85C341', // ফিগমা থেকে হেক্স কোডটা মিলিয়ে নিও
      },
    },
  },
};

export default config;
